import { createServerClient } from "@supabase/ssr"; import type { Database } from "$lib/types/supabase"; import { redirect } from "@sveltejs/kit"; import type { Cookies } from "@sveltejs/kit"; export const createSupabaseServerClient = async ({ cookies, fetch }: { cookies: Cookies; fetch: typeof globalThis.fetch }) => { const supabase = createServerClient<Database>(import.meta.env.PUBLIC_SUPABASE_URL, import.meta.env.PUBLIC_SUPABASE_ANON_KEY, { cookies: { get: (key) => cookies.get(key), set: (key, value, options) => { cookies.set(key, value, { ...options, path: "/" }); }, remove: (key, options) => { cookies.delete(key, { ...options, path: "/" }); } }, global: { fetch } }); return supabase; };
