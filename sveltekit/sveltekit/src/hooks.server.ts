import { createSupabaseServerClient } from "$lib/supabase/server"; import type { Handle } from "@sveltejs/kit"; export const handle: Handle = async ({ event, resolve }) => { event.locals.supabase = await createSupabaseServerClient({ cookies: event.cookies, fetch: event.fetch }); event.locals.getSession = async () => { const { data: { session } } = await event.locals.supabase.auth.getSession(); return session; }; return resolve(event, { filterSerializedResponseHeaders: (name) => name === "content-range" }); };
